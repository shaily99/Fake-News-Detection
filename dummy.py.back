import sys
import random

n = int(sys.argv[1])
p = 2
while p < n:
        for i in range(2, p):
                if p%i == 0:
                        p=p+1 
        print "%s" % p,
        p=p+1

#sys.exit(0)
sys.exit(random.randint(0,1))

